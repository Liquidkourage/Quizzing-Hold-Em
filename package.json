{
  "name": "quizzem",
  "private": true,
  "version": "0.1.0",
  "description": "Quizz'em — real-time trivia + Hold'em hybrid; host, player, display apps and Socket.IO server.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=9"
  },
  "scripts": {
    "dev": "concurrently -n server,host,player,display -c cyan,magenta,green,yellow \"npm --workspace apps/server run dev\" \"npm --workspace apps/host run dev\" \"npm --workspace apps/player run dev\" \"npm --workspace apps/display run dev\"",
    "build": "npm run build --workspace packages/core && npm run build --workspace packages/net && npm run build --workspace packages/ui && npm run build --workspace apps/server && npm run build --workspace apps/host && npm run build --workspace apps/player && npm run build --workspace apps/display",
    "start": "npm start --workspace apps/server",
    "serve": "npm start --workspace apps/server",
    "open": "powershell -Command Start-Process http://localhost:7777",
    "one": "npm run build && powershell -Command Start-Process http://localhost:7777 && npm start --workspace apps/server",
    "lint": "npm run lint --workspace apps/host && npm run lint --workspace apps/player && npm run lint --workspace apps/display",
    "typecheck": "tsc -b packages/core/tsconfig.json packages/net/tsconfig.json packages/ui/tsconfig.json apps/server/tsconfig.json apps/host/tsconfig.json apps/player/tsconfig.json apps/display/tsconfig.json",
    "test": "npm run test --workspace @qhe/core",
    "format": "prettier --write .",
    "trim-logo": "node scripts/trim-quizzem-logo.mjs",
    "sim:lobby": "node scripts/sim-venue-lobby-joiners.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "concurrently": "^9.0.1",
    "eslint": "^9.7.0",
    "prettier": "^3.3.3",
    "sharp": "^0.34.2",
    "typescript": "^5.6.2"
  }
}
